import React from 'react';
import GameEmbed from './GameEmbed';

const FallingBall: React.FC = () => {
  return (
    <GameEmbed slug="falling-ball" fullScreen={true} />
  );
};

export default FallingBall;
